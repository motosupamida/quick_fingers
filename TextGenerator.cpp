#include "TextGenerator.h"

std::wstring TextGenerator::getRandomText(const unsigned short stringSize)
{
    //std::wstring result = L"";
    //srand(time(NULL));
    //for (unsigned short i = 0; i < stringSize; i++)
    //{
    //    result += (160 + rand() % 85);
    //}
    //return result;
    return L"Ѕольшой тестовый текст дл€ проверки работоспособности данной замечательной программы по вычислению скорости печати";
}

std::wstring TextGenerator::getTextFromFile()
{
    //std::wstring result = L"";
    //return result;
    return L"“естовый текст";
}
